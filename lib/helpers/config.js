'use strict';

var Config = {
    startUp: {
        title: 'Setup initial visibility',
        description: 'Set if **project-viewer** panel should be visible from the start up',
        type: 'boolean',
        default: false
    }
};

// the buffer option
// set path and name of the db file

module.exports = Config;
