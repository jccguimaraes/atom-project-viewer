'use strict';

const project = Object.create(null);

project.listAll = function _listAll () {
    return [];
};

project.listByClient = function _listByClient () {
    return [];
};

project.listByGroup = function _listByGroup () {
    return [];
};

module.exports = project;
